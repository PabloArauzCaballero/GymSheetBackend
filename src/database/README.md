# database

Configura Sequelize y registra los modelos del dominio.

La sincronización automática está desactivada porque el proyecto está pensado para producción. El esquema base está documentado en `docs/db/schema.sql`.
