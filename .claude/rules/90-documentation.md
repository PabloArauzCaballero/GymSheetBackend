---
paths:
  - "docs/**"
  - "**/*.md"
---

# Documentación

- Toda conclusión técnica se sustenta en evidencia ejecutada (comando + resultado). No inventes
  métricas que no se midieron.
- Las decisiones de arquitectura y de dependencias van en ADRs (`docs/decisions/`).
- Mantén `CLAUDE.md` breve y estable; los procedimientos largos van a `.claude/skills/`, no al
  `CLAUDE.md`.
- No copies documentos extensos ni protegidos; resume principios y conserva la atribución.
- El documento que refleja el estado de producción es `BACKEND_AUDIT_HARDENING_AND_ACTION_PLAN.md`;
  manténlo alineado con el código final.
